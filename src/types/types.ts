export interface ButtonProps {
    title: string;
    background?: string;
    scrolled?: boolean;
}

export interface CoursesProps {
    type: string;
    title: string;
    time: string;
    link: string;
    color: string;
    btntext?: string;
    textcolor: string;
    position: string;
    // could change to number
}